# Database connection string.
# Defaults to local SQLite if left empty or commented out.
# For production hosting (e.g. Render, Railway, Neon, Supabase), uncomment and set this to your hosted PostgreSQL database URL:
# DATABASE_URL=postgresql://user:password@hostname:5432/dbname
DATABASE_URL=sqlite:///./airbnb.db
